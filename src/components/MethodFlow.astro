<div
  class="not-prose my-10 grid gap-3 md:grid-cols-[1fr_auto_1fr_auto_1fr] md:items-stretch"
>
  <div class="method-step">
    <span>01</span>
    <h3>Generate a block</h3>
    <p>
      Near-term actions reach clean states first while later blocks retain their
      intermediate states.
    </p>
  </div>
  <div class="method-arrow" aria-hidden="true">→</div>
  <div class="method-step">
    <span>02</span>
    <h3>Execute & sense</h3>
    <p>
      The ready block is executed, then fresh tactile observations are encoded
      from the fingertips.
    </p>
  </div>
  <div class="method-arrow" aria-hidden="true">→</div>
  <div class="method-step">
    <span>03</span>
    <h3>Resume with touch</h3>
    <p>
      EATA routes the latest tactile tokens only to the action block scheduled
      to execute next.
    </p>
  </div>
</div>

<style>
  .method-step {
    border: 1px solid color-mix(in srgb, var(--tac-blue) 28%, transparent);
    border-radius: 1rem;
    padding: 1.25rem;
    background: linear-gradient(
      145deg,
      color-mix(in srgb, var(--tac-blue) 8%, white),
      white
    );
  }
  .method-step span {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2rem;
    height: 2rem;
    border-radius: 999px;
    color: white;
    background: var(--tac-blue-dark);
    font-size: 0.75rem;
    font-weight: 800;
    letter-spacing: 0.08em;
  }
  .method-step h3 {
    margin: 1rem 0 0.4rem;
    color: var(--color-zinc-900);
    font-size: 1.05rem;
  }
  .method-step p {
    margin: 0;
    color: var(--color-zinc-600);
    font-size: 0.9rem;
    line-height: 1.55;
  }
  .method-arrow {
    display: none;
    align-items: center;
    color: var(--tac-orange);
    font-size: 1.75rem;
    font-weight: 700;
  }
  @media (min-width: 768px) {
    .method-arrow {
      display: flex;
    }
  }
  :global([data-theme="dark"]) .method-step {
    border-color: color-mix(in srgb, var(--tac-blue) 42%, transparent);
    background: linear-gradient(
      145deg,
      color-mix(in srgb, var(--tac-blue) 15%, var(--color-zinc-900)),
      var(--color-zinc-900)
    );
  }
  :global([data-theme="dark"]) .method-step h3 {
    color: white;
  }
  :global([data-theme="dark"]) .method-step p {
    color: var(--color-zinc-300);
  }
</style>
